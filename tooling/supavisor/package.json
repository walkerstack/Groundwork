{
  "name": "@agentset/supavisor",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "docker-compose up",
    "create-tenant": "pnpm dlx tsx --env-file .env create-tenant.ts"
  },
  "devDependencies": {
    "pino": "^9.7.0",
    "pino-pretty": "^13.0.0",
    "testcontainers": "^11.0.0"
  }
}